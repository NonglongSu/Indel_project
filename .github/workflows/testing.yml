on: push


jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        R: [ '4.2.1' ]
    name: R ${{ matrix.R }} unitTest
    steps:
      - run: |
          source("../../Script/tests/coverage_report.R")
        shell: Rscript {0}


